'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { cn } from '@/lib/utils'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import {
  Sparkles,
  LayoutDashboard,
  Mail,
  Bookmark,
  Settings,
  Star,
  Zap,
  Gift,
  Crown,
} from 'lucide-react'

const navigation = [
  {
    name: 'My Digest',
    href: '/dashboard',
    icon: LayoutDashboard,
    description: 'Your magical newsletter dashboard'
  },
  {
    name: 'All Newsletters',
    href: '/newsletters',
    icon: Mail,
    description: 'Manage your newsletter subscriptions'
  },
  {
    name: 'Treasure Chest',
    href: '/saved',
    icon: Bookmark,
    description: 'Your saved article collection'
  },
  {
    name: 'Magic Categories',
    href: '/categories',
    icon: Star,
    description: 'Organize with smart categories'
  },
  {
    name: 'Genie Settings',
    href: '/settings',
    icon: Settings,
    description: 'Customize your magical experience'
  },
]

const magicalFeatures = [
  {
    name: 'Pro Magic',
    href: '/upgrade',
    icon: Crown,
    badge: 'Upgrade',
    badgeColor: 'bg-magic-100 text-magic-700'
  },
  {
    name: 'Refer Friends',
    href: '/referrals',
    icon: Gift,
    badge: 'Earn',
    badgeColor: 'bg-emerald-100 text-emerald-700'
  },
]

export function Sidebar() {
  const pathname = usePathname()

  return (
    <div className="w-64 bg-white border-r border-gray-200 flex flex-col">
      {/* DigestGenie Logo */}
      <div className="p-6 border-b border-gray-200">
        <Link href="/dashboard" className="flex items-center space-x-3 group">
          <div className="w-10 h-10 bg-gradient-to-br from-genie-500 to-magic-500 rounded-xl flex items-center justify-center group-hover:animate-genie-sparkle">
            <Sparkles className="w-6 h-6 text-white" />
          </div>
          <div>
            <h1 className="font-display font-bold text-xl text-gray-900">
              DigestGenie
            </h1>
            <p className="text-xs text-gray-500">Your AI Newsletter Genie</p>
          </div>
        </Link>
      </div>

      {/* Main Navigation */}
      <nav className="flex-1 p-4 space-y-1">
        <div className="mb-6">
          <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-3">
            ✨ Genie Dashboard
          </p>
          {navigation.map((item) => (
            <Link
              key={item.name}
              href={item.href}
              className={cn(
                'group flex items-center px-3 py-2 text-sm font-medium rounded-lg transition-all duration-200',
                pathname === item.href
                  ? 'bg-gradient-to-r from-genie-50 to-magic-50 text-genie-700 border-r-2 border-genie-500'
                  : 'text-gray-600 hover:bg-gray-50 hover:text-genie-600'
              )}
            >
              <item.icon
                className={cn(
                  'mr-3 h-5 w-5 transition-colors',
                  pathname === item.href ? 'text-genie-600' : 'text-gray-400 group-hover:text-genie-500'
                )}
              />
              <div className="flex-1">
                <div className="font-medium">{item.name}</div>
                <div className="text-xs text-gray-500 mt-0.5">
                  {item.description}
                </div>
              </div>
            </Link>
          ))}
        </div>

        {/* Magical Features */}
        <div>
          <p className="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-3">
            🪄 More Magic
          </p>
          {magicalFeatures.map((item) => (
            <Link
              key={item.name}
              href={item.href}
              className="group flex items-center px-3 py-2 text-sm font-medium rounded-lg text-gray-600 hover:bg-gradient-to-r hover:from-magic-50 hover:to-genie-50 hover:text-genie-600 transition-all duration-200"
            >
              <item.icon className="mr-3 h-5 w-5 text-gray-400 group-hover:text-magic-500 transition-colors" />
              <span className="flex-1">{item.name}</span>
              {item.badge && (
                <Badge className={cn('text-xs', item.badgeColor)}>
                  {item.badge}
                </Badge>
              )}
            </Link>
          ))}
        </div>
      </nav>

      {/* Magic Stats */}
      <div className="p-4 border-t border-gray-200">
        <div className="bg-gradient-to-br from-genie-50 to-magic-50 rounded-lg p-4">
          <div className="flex items-center justify-between mb-3">
            <span className="text-sm font-medium text-genie-700">Magic Level</span>
            <Zap className="w-4 h-4 text-magic-500" />
          </div>
          <div className="w-full bg-white rounded-full h-2 mb-2">
            <div className="bg-gradient-to-r from-genie-500 to-magic-500 h-2 rounded-full w-3/4"></div>
          </div>
          <p className="text-xs text-gray-600">
            <span className="font-medium text-genie-600">157</span> newsletters organized
          </p>
          <p className="text-xs text-gray-600">
            <span className="font-medium text-magic-600">2.3h</span> saved this week
          </p>
        </div>
      </div>
    </div>
  )
}